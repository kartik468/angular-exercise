import { CustomMaxLengthDirective } from './custom-max-length.directive';

describe('CustomMaxLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomMaxLengthDirective();
    expect(directive).toBeTruthy();
  });
});
